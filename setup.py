from setuptools import setup, find_packages

setup(
    name='BenjaminsPackage',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # List your package's dependencies here, e.g.,
        # 'numpy',
        # 'pandas',
    ],
)
